---
name: report
description: 입찰 분석 결과를 한글 리포트로 변환. analysis_*.json 입력 → 1위 확률 중심 해석 → report_*.md 생성. 공고정보, 몬테카를로, 경쟁밀도, 패턴분석, 최적 입찰률 3개 제시. Use when 분석 완료 후 결과 확인, 리포트 생성, 한글 해석
---

# 입찰 분석 리포트 생성

## ⚠️ 이 문서는 분석 결과 해석 방법입니다

> **1위 확률 = 경쟁 밀도 역수**
>
> 경쟁자 n명 → 1위 확률 = 1/(n+1) × 100%
>
> 예시 (실제 값은 분석마다 다름):
> - 경쟁자 적은 구간 → 1위 확률 높음
> - 경쟁자 많은 구간 → 1위 확률 낮음

═══

## 1. 리포트 구조

### 전체 흐름

```
1. 📋 공고 정보
   - 기초금액, 발주처투찰률

2. 🎲 몬테카를로 시뮬레이션
   - 예정가 분포 ({데이터_기반_계산})
   - 대수의 법칙으로 중앙 수렴

3. 📊 과거 1위 분석
   - 1위 분포 참고 (중앙값: {실제_데이터})
   - ⚠️ 1위 많은 곳 ≠ 최적

4. 🔥 경쟁 밀도 분석 (핵심!)
   - 0.05% 구간별 경쟁자 수
   - 회피 구간 (고밀도) vs 안전 구간 (저밀도)
   - 1위 확률 = 1 / (경쟁자+1)

5. 🔢 소수점/끝자리 패턴
   - 차별화 포인트
   - 000, 500 회피

6. 🛡️ 하한가 미달 분석
   - 미달 {실제_비율}% (무시)
   - 미달 = 리스크 아님

7. 📈 사정율-투찰율 상관관계
   - 상관계수 {실제_계산값}
   - 데이터 기반 상관관계 분석

8. 🎯 최적 입찰률 3개
   - 기대값 최적 (균형)
   - 경쟁 회피 (1위 확률 최대)
   - 이익 최대 (수익 우선)
```

═══

## 2. 섹션별 해석 방법

### 2.1. 공고 정보

```markdown
## 📋 공고 정보

- **기초금액:** {공고에서_추출}원
- **발주처투찰률:** {공고에서_추출}%
```

**해석**:
- 기초금액 = 입찰의 기준점
- 발주처투찰률 = 예정가격 기준 최소 낙찰률
- **중요**: 예정가격 기준 (기초금액 아님!)

⚠️ **모든 값은 공고마다 다름**

### 2.2. 몬테카를로 시뮬레이션

```markdown
## 🎲 몬테카를로 시뮬레이션

- **예정가격 평균:** {분석_결과}원 (기초 × {계산값}%)
- **예정가격 범위:** {최소값} ~ {최대값}원
- **낙찰하한가 평균:** {분석_결과}원 (기초 × 발주처투찰률)
- **낙찰하한가 범위:** {최소값} ~ {최대값}원
- **기초대비 낙찰하한율 범위:** {최소}% ~ {최대}%

**💡 해석:**
예정가격 무작위성으로 인해 넓은 범위에서 낙찰 가능.
대수의 법칙으로 중앙(발주처투찰률) 근처 형성 확률 높음.
```

**해석 포인트**:
- 예정가 무작위 → 낙찰하한가도 무작위
- 실제 낙찰 가능 범위는 데이터 분석으로 계산
- 중앙값 근처 형성 확률 가장 높음

⚠️ **모든 값은 공고마다 다름**

### 2.3. 과거 1위 분석

```markdown
## 📊 과거 1위 분석

- **분석 데이터:** {데이터_건수}개
- **평균:** {실제_계산}%
- **중앙값:** {실제_계산}%
- **표준편차:** {실제_계산}%
- **범위:** {최소}% ~ {최대}%

**백분위수:**
- 25%: {실제_계산}%
- 50%: {실제_계산}%
- 75%: {실제_계산}%

**💡 해석:**
1위가 특정 범위에 몰려있음.
BUT 중앙값 근처 = 경쟁 최고 밀집!
→ "1위 분포 ≠ 최적 전략"
```

**해석 포인트**:
- 1위가 많은 곳 = 경쟁도 많은 곳
- 중앙값 ≠ 추천 입찰률
- 참고용으로만 사용

⚠️ **모든 값은 공고마다 다름**

### 2.4. 경쟁 밀도 분석 (핵심!)

```markdown
## 🔥 경쟁 밀도 분석 (핵심!)

### ⚠️ 절대 회피 구간 (고밀도)

**최고 밀집:** {구간}: {경쟁자수}명

**회피 구간 Top 5:**
1. {구간}: {경쟁자수}명 → 1위 확률 {계산값}% ❌
2. {구간}: {경쟁자수}명 → 1위 확률 {계산값}%
3. {구간}: {경쟁자수}명 → 1위 확률 {계산값}%
4. {구간}: {경쟁자수}명 → 1위 확률 {계산값}%
5. {구간}: {경쟁자수}명 → 1위 확률 {계산값}%

### ✅ 안전 구간 (저경쟁)

**안전 구간 Top 5:**
1. {구간}: {경쟁자수}명 → 1위 확률 {계산값}% ✅
2. {구간}: {경쟁자수}명 → 1위 확률 {계산값}%
3. {구간}: {경쟁자수}명 → 1위 확률 {계산값}%
4. {구간}: {경쟁자수}명 → 1위 확률 {계산값}%
5. {구간}: {경쟁자수}명 → 1위 확률 {계산값}%

**💡 해석:**
고밀도 구간 vs 저밀도 구간 = 1위 확률 큰 차이!
안전 구간 선택 시 충돌 위험 최소화.
```

**해석 포인트**:
- **1위 확률 = 1 / (경쟁자 + 1)**
- 경쟁자 적을수록 1위 확률 높음
- 경쟁 밀도 낮을수록 1위 확률 기하급수적 증가

⚠️ **모든 값은 공고마다 다름**

### 2.5. 소수점/끝자리 패턴

```markdown
## 🔢 소수점 패턴

**회피할 숫자 (빈도 높음):**
- {숫자}: {빈도}개
- {숫자}: {빈도}개
- {숫자}: {빈도}개

**안전한 숫자 (빈도 낮음):**
- {숫자}: {빈도}개
- {숫자}: {빈도}개
- {숫자}: {빈도}개

**💡 해석:**
소수점 3자리만 바꿔도 충돌 확률 차이 발생.

## 끝자리 선호도

**회피할 끝자리:**
- {끝자리}원: {빈도}개
- 000원: {빈도}개 ← 직관적 숫자 회피!
- 500원: {빈도}개

**안전한 끝자리:**
- {끝자리}원: {빈도}개
- {끝자리}원: {빈도}개
- {끝자리}원: {빈도}개

**💡 해석:**
000, 500 같은 직관적 숫자는 빈도 높음.
절대 회피!
```

**해석 포인트**:
- 차별화 전략
- 직관적 숫자 (0, 5) 회피
- 홀수 끝자리 선호

⚠️ **모든 값은 공고마다 다름**

### 2.6. 하한가 미달 분석

```markdown
## 🛡️ 하한가 미달 분석

- **전체 데이터:** {데이터_건수}개
- **하한가 미달:** {건수}개 ({비율}%)
- **1위:** {건수}개 ({비율}%)

**미달 업체 투찰률:**
- 평균: {실제_계산}%
- 중앙값: {실제_계산}%
- 최대: {실제_계산}% ← 높게 써도 미달 가능!

**💡 해석:**
미달 vs 1위 큰 차이
BUT 미달 = 리스크 아님 (다음 기회)
→ 1위 확률만 최대화!
```

**해석 포인트**:
- 미달 확률 무시
- 높게 입찰해도 미달 가능 (예정가 낮게 형성 시)
- 예정가 형성 결과에 따라 결정됨
- **1위 확률만 중요**

⚠️ **모든 값은 공고마다 다름**

### 2.7. 사정율-투찰율 상관관계

```markdown
## 📈 사정율-투찰율 상관관계

- **상관계수:** {실제_계산} (상관 강도)
- **선형 회귀:** 투찰률 = 사정률 × {계수} + {절편}

**예측:**
- 사정률 변화 시 투찰률도 변화
- 데이터 기반 회귀 분석

**💡 해석:**
사정률과 투찰률 간 상관관계 존재.
업체들이 사정률 보고 조정하는 경향.
```

**해석 포인트**:
- 기초대비사정률 & 투찰률 = 같은 스케일 (100분율)
- 항상 함께 표시
- 상관관계로 업체 심리 파악

⚠️ **모든 값은 공고마다 다름**

### 2.8. 최적 입찰률 3개

```markdown
## 🎯 최적 입찰률 3개

| 순위 | 전략명 | 입찰률 | 입찰금액 | 경쟁자 | 1위 확률 | 종합점수 |
|:----:|--------|-------:|---------:|-------:|---------:|---------:|
| 1 | 전략 A | {분석}% | {분석}원 | {분석}명 | {분석}% | {분석} |
| 2 | 전략 B | {분석}% | {분석}원 | {분석}명 | {분석}% | {분석} |
| 3 | 전략 C | {분석}% | {분석}원 | {분석}명 | {분석}% | {분석} |

### 전략별 상세

#### 1. 전략 A (예: 종합 점수 최적)
- **추천 이유:** 데이터 기반 종합 점수 최대
- **장점:** 균형잡힌 선택
- **단점:** 데이터에 따라 다름

#### 2. 전략 B (예: 경쟁 회피)
- **추천 이유:** 경쟁 밀도 최소
- **장점:** 1위 확률 최대 ✅
- **단점:** 예정가 형성 조건 필요

#### 3. 전략 C (예: 보수적)
- **추천 이유:** 안전한 선택
- **결과:** 데이터에 따라 다름

**💡 최종 추천:**
분석 결과에 따라 최적 전략 제시
- 경쟁자 수, 1위 확률, 종합 점수 종합 고려
```

**해석 포인트**:
- **1위 확률 비교가 핵심**
- 경쟁자 수 → 1위 확률 직접 계산
- 미달 확률 표시 안 함 (무의미)
- 여러 전략 trade-off 비교

⚠️ **모든 값은 공고마다 다름**

═══

## 3. 리포트 생성 코드 (의사 코드)

```python
def generate_report(analysis_json):
    """
    JSON 분석 결과 → 한글 리포트

    핵심:
    - 1위 확률 = 1 / (경쟁자 + 1)
    - 미달 확률 무시
    - 기초대비사정률-투찰률 동시 표시
    """
    report = []

    # 1. 공고 정보
    report.append(f"기초금액: {analysis['base_amount']:,}원")
    report.append(f"발주처투찰률: {analysis['agency_rate']}%")

    # 2. 몬테카를로
    mc = analysis['phase1_monte_carlo']
    report.append(f"예정가 범위: {mc['min']}~{mc['max']}%")

    # 3. 과거 1위
    past = analysis['phase2_past_winners']
    report.append(f"1위 중앙값: {past['median']}%")
    report.append("⚠️ 1위 많은 곳 ≠ 최적")

    # 4. 경쟁 밀도 (핵심!)
    density = analysis['phase3_density_heatmap']
    for bin, count in sorted(density.items(), key=lambda x: x[1], reverse=True)[:5]:
        p_win = 1 / (count + 1) * 100
        report.append(f"{bin}: {count}명 → 1위 확률 {p_win:.2f}%")

    # 5. 미달
    below = analysis['phase6_below_minimum']
    report.append(f"미달 {below['rate']:.1f}% (무시)")

    # 6. 사정율-투찰율
    corr = analysis['phase7_correlation']
    report.append(f"상관계수: {corr['correlation']:.2f}")

    # 7. 최적 입찰률
    opt = analysis['phase9_optimization']
    for i, strategy in enumerate(['optimal_1', 'optimal_2', 'optimal_3'], 1):
        s = opt[strategy]
        p_win = 1 / (s['competitors'] + 1) * 100
        report.append(f"{i}. {s['name']}")
        report.append(f"   입찰률: {s['rate']}%")
        report.append(f"   경쟁자: {s['competitors']}명")
        report.append(f"   1위 확률: {p_win:.2f}%")

    return "\n".join(report)
```

═══

## 4. 핵심 메시지 (강조 사항)

### 리포트에 반드시 포함할 내용

```markdown
## 💡 핵심 원칙

1. **1위 아니면 의미 없음**
   - 2위 = 미달 = 0원
   - 평균가(안전지대) = 안전하게 탈락

2. **미달 = 리스크 아님**
   - 미달 비율 높아도 문제없음
   - 실패하면 다음 기회
   - 1위 확률만 최대화

3. **경쟁 밀도 역수 = 1위 확률**
   - 경쟁자 n명 → 1위 확률 = 1/(n+1)
   - 경쟁자 적을수록 1위 확률 기하급수적 증가
   - 고밀도 vs 저밀도 구간 차이 큼

4. **군중 피하기**
   - 최적해를 모두가 찾으면 → 최악
   - 무질서 속에 기회
   - 저밀도 구간 선택
```

⚠️ **모든 수치는 공고마다 다름**

### 회피 사항 체크리스트

```markdown
## ⚠️ 핵심 회피 사항

- [ ] 고밀도 구간 (데이터 분석 결과) 절대 회피
- [ ] 소수점 빈도 높은 숫자 회피 (데이터 기반)
- [ ] 끝자리 000, 500 회피 (직관적 숫자)
- [ ] 고밀도 구간 회피 (데이터 기반)
- [ ] 1위 중앙값 근처 회피 (경쟁 최고)
```

⚠️ **구체적 수치는 공고마다 다름**

═══

## 5. 참고 문서

**필수 참조 순서:**
1. `/mnt/a/25/.claude/skills/core/SKILL.md` - 철학
2. `/mnt/a/25/.claude/skills/logic/SKILL.md` - 알고리즘
3. `/mnt/a/25/CLAUDE.md` - 프로젝트 가이드

**Last Updated:** 2025-10-26
**Philosophy:** 1위 확률 중심 해석
